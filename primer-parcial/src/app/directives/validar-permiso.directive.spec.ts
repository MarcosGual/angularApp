import { ValidarPermisoDirective } from './validar-permiso.directive';

describe('ValidarPermisoDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidarPermisoDirective();
    expect(directive).toBeTruthy();
  });
});
